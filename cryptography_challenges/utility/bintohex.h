#include <sstream>
#include <string>
#include <iostream>

typedef std::string string;
typedef std::stringstream sstream;

void binSegmentToHex(string hexSegment, sstream *ss);
void binToHex(string bin, sstream *ss);